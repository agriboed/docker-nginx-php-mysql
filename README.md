**** Nginx + PHP7.3 + Composer + Xdebug 2.7 + MySQL 5.7 using Docker ****

* Install Docker and Docker-compose then run command in CLI:

`docker-compose build`

* Then run:

`docker-compose up -d`

* After open your browser with:

`http://localhost/`